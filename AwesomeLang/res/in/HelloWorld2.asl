import stdio.h;

int main() {
	string testharder = "so hard";
	printf("Hello import " + "a" + stringmethod() + "World!" + testharder);
	printf(testharder + stringmethod())
	return 0;
}

string stringmethod() {
	return "test";
}
